#include <stdio.h>

int main() {
    int count = 0;

    // Example of a while loop
    while (count < 5) {
        printf("Count is: %d\n", count);
        count++;
    }

    printf("Loop finished. Final count is: %d\n", count);

    return 0;
}